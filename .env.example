# SortSmith currently requires no environment variables or secrets.
# Add future non-secret configuration keys here only after the application reads and documents them.
# Never place real credentials, signing material, personal data, or production secrets in this file.
